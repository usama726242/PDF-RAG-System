# PDF-RAG-System
PDF-RAG-System is a production-ready RAG system built in n8n. PDFs are automatically parsed, chunked, embedded, and stored in ChromaDB. User queries retrieve relevant context, enabling accurate AI responses with source attribution. Features include session memory, error handling, and hallucination prevention.
